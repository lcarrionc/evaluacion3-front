import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['picsum.photos', 'i.dummyjson.com', 'fakestoreapi.com', 'api.escuelajs.co'],
  },
};

export default nextConfig;
